---
apiVersion: "networking.k8s.io/v1beta1"
kind: "Ingress"
metadata:
  name: "cmap-service"
  namespace: "abuse-api-ote"
spec:
  tls:
    - hosts:
      - "cmapservice.int.ote-godaddy.com"
      secretName: tls-cmap-service
  rules:
  - host: "cmapservice.int.ote-godaddy.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: "cmap-service"
          servicePort: 5000

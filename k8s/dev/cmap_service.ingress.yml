---
apiVersion: "extensions/v1beta1"
kind: "Ingress"
metadata:
  name: "cmap-service"
  namespace: "abuse-api-dev"
spec:
  tls:
    - hosts:
      - "cmapservice.int.dev-godaddy.com"
      secretName: tls-cmap-service
  rules:
  - host: "cmapservice.int.dev-godaddy.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: "cmap-service"
          servicePort: 5000
